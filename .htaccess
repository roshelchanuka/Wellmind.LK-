<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase "/final project/"
    
    # Send Requests To index.php...
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php [L]
</IfModule>
